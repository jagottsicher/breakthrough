# Local-only development file, never committed (see local dev notes).
CLAUDE.md

# Fine-grained GitHub PAT, local-only, never committed.
github-token.txt

# Claude Code local state: personal permission overrides (Claude Code's
# own convention: the ".local" suffix marks this as machine-specific,
# never meant for version control) and worktree checkouts created for
# background/parallel sessions — never committed, same "local-only
# tooling artifact" principle as CLAUDE.md/github-token.txt above.
.claude/settings.local.json
.claude/worktrees/

# Go build artifacts
/bin/
*.test
*.out

# GoReleaser build output
dist/
